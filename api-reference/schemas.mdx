---
title: 'Schemas'
description: 'Zod schema reference for the EduLearn Hub API — generated by Orval from the OpenAPI spec.'
keywords: ['zod', 'schemas', 'validation', 'orval', 'generated', 'typescript', 'types']
---

All Zod schemas are auto-generated by [Orval](https://orval.dev) from `lib/api-spec/openapi.yaml` and published under the `@workspace/api-zod` package. Do not edit the generated files directly — update the OpenAPI spec and run codegen instead.

<Note>
Generated files live in `lib/api-zod/src/generated/`. To regenerate them, see [Codegen](/development/codegen).
</Note>

## Import path

```typescript
import { HealthCheckResponse } from "@workspace/api-zod";
```

## `HealthCheckResponse`

The Zod schema for the `GET /api/healthz` response body, generated from the `HealthStatus` object defined in the OpenAPI spec.

### Schema definition

<ResponseField name="status" type="string" required>
  Health status string returned by the server.
</ResponseField>

### Generated code

```typescript lib/api-zod/src/generated/api.ts
/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
import * as zod from "zod";

/**
 * Returns server health status
 * @summary Health check
 */
export const HealthCheckResponse = zod.object({
  status: zod.string(),
});
```

## Using `HealthCheckResponse`

### Runtime validation

Use `.parse()` to validate data at runtime. Throws a `ZodError` if the data does not match the schema.

```typescript
import { HealthCheckResponse } from "@workspace/api-zod";

const data = HealthCheckResponse.parse({ status: "ok" });
// data is { status: string }
```

### Safe parsing

Use `.safeParse()` to validate without throwing. Returns a discriminated union with `success`, `data`, or `error`.

```typescript
import { HealthCheckResponse } from "@workspace/api-zod";

const result = HealthCheckResponse.safeParse(unknownData);

if (result.success) {
  console.log(result.data.status);
} else {
  console.error(result.error);
}
```

### TypeScript type inference

Derive the TypeScript type from the schema using `z.infer` to keep the type and schema permanently in sync:

```typescript
import { z } from "zod";
import { HealthCheckResponse } from "@workspace/api-zod";

type HealthStatus = z.infer<typeof HealthCheckResponse>;
// { status: string }
```

## OpenAPI source

The `HealthStatus` schema is defined in `lib/api-spec/openapi.yaml`:

```yaml lib/api-spec/openapi.yaml
components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
```

## TypeScript interface

Orval also generates a plain TypeScript interface alongside the Zod schema, available from `@workspace/api-client-react`:

```typescript lib/api-client-react/src/generated/api.schemas.ts
/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}
```

<Tip>
Use the Zod schema (`HealthCheckResponse`) when you need runtime validation. Use the TypeScript interface (`HealthStatus`) when you only need a type annotation and want to avoid importing the schema object.
</Tip>
